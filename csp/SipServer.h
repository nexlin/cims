#ifndef _SIP_SERVER_H_
#define _SIP_SERVER_H_

#include "SipUserAgent.h"

// CSipServer 는 ModuleDispatcher 로 대체됨
// gclsUserAgent 만 유지 (전역 SipUserAgent 인스턴스)

extern CSipUserAgent gclsUserAgent;

#endif
