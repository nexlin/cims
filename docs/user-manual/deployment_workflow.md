# Console 배포 작업 순서

Console 에서 새 서버에 모듈을 배포하고 서비스 시작까지의 표준 절차.

## 0. 전제

- base 운영평면(oam/console/agent) 구동 중 — 부트스트랩은 `design/02_deployment.md` §2.1
- Console 로그인 (`https://<OAM>:4419/`) — operator 이상. 시스템 구성/패키지 설치·제어의
  **변경**은 admin 세션 또는 우측 상단 [🔐 관리자 인증](admin 패스워드 30분 승격) 필요

> 배포·운영의 primary 화면은 **관리 > 시스템 > 시스템/인프라** (`/deploy/servers`) 하나다.
> 상단 탭 4개가 라이프사이클 단계를 가른다:
> **[시스템/서버 구성]**(서버·HA·네트워크) → **[패키지 설치]**(파일 배치) →
> **[패키지 설정]**(설정 편집) → **[패키지 제어]**(프로세스 시작/정지).
> 좌측 서버 목록에서 무엇을 골랐는지(그룹 vs 서버)에 따라 우측 상세 패널 내용이 바뀐다.

## 1. 패키지 업로드

**메뉴**: `관리 > 시스템 > 패키지` (`/deploy/packages`, admin)

1. `＋ 패키지 업로드` 클릭
2. 빌드된 tarball (`<module>-<version>.tar.gz`) 을 선택 (여러 개 동시 가능)
3. "업로드" → 완료될 때까지 대기
4. 좌측 모듈 목록에서 업로드된 모듈이 보임 (버전 수 뱃지 표시)
5. 버전 카드 펼치면 `config_template.json` 유무 · 크기 · SHA256 확인 가능

**재업로드**: 동일 (모듈명, 버전) 업로드 시 자동으로 덮어쓰기.

> CIMS 자체에서 빌드한 tarball 은 `릴리스 > 패키징` (`/release/package`)
> 카드의 ⤓ 다운로드 버튼으로 받아서 그대로 위 화면에 업로드한다. 자세한
> 워크플로우는 `design/features/build_and_packaging.md` 참고.

## 2. 시스템(서버) 등록 — [시스템/서버 구성] 탭

**메뉴**: `시스템/인프라` → 상단 [시스템/서버 구성] 탭

1. 좌측 서버 목록 하단 `＋ 시스템 추가` 클릭. 이름 입력 + 유형 선택:
   - **A/S** — master + backup 서버 2개 자동 발급 (예: SIP 서버 이중화)
   - **AA** — 시작 1개 발급, 트리의 [+] 로 N개 확장 (예: 미디어 서버 분산)
   - **SA** — 단일 서버
2. 생성되면 좌측 트리에 그룹/서버가 나타난다. 서버 선택 → 우측 "설치 안내" 섹션의
   install command 를 복사해 대상 호스트에서 실행:
   ```bash
   curl -k https://<OAM>:4419/install-agent.sh | bash -s -- \
     --oam-url https://<OAM>:4419 \
     --enrollment-token <TOKEN> \
     --name <이름>
   # 이후 대상 호스트에서 sudo ./install.sh
   ```
3. agent enroll 완료 → `pending → online` 자동 전환 + 인터페이스 자동 보고
4. 서버 선택 → "네트워크" 섹션에서 IP/Routing · 마운트 · 네트워크 튜닝(RPS/sysctl) 설정
5. (A/S·AA) 그룹 선택 → "절체 조건" · "멤버" · "VIP Bindings" 표에서 HA 구성 → 각 영역의
   [▶ 적용] 으로 반영 (keepalived 즉시 재적용)

## 3. 모듈 배포 — [패키지 설치] 탭

**위치**: [패키지 설치] 탭 → 서버 선택 → 모듈 목록 → `＋ 모듈 추가`

1. **모듈** 선택 (예: `csp`, `psp`, `cmp`, `pmp`, ...)
   - 같은 base 바이너리의 변종은 별도 패키지로 노출 (csp/psp/isp / cmp/pmp/imp)
   - HA 그룹 멤버면 패키지의 `ha_capability` 와 그룹 mode 가 맞아야 선택 가능
2. **버전** 선택 (기본 = 최신 업로드)
3. **모듈 이름** — 프로세스 이름 (기본 = 패키지 meta 의 process, 예: `CSP`)
4. 메모 (선택) → `추가`

→ Deployment 가 `pending` 상태로 생성됨. 아직 파일 없음.

모듈 행의 작업 버튼은 **파일 배치**만 담당한다: `설치/재설치` · `↑ 업그레이드`(다른 버전
패키지가 등록돼 있을 때) · `⤺ 롤백`(이전 버전 보존 시) · `✕`(제거). 그룹을 선택하면
멤버별 배포 현황을 읽기 전용으로 보여준다.

**설치**: 모듈 행 → `설치` — `install` job 큐잉 → Agent 가 heartbeat(30s) 시 pickup.
완료까지 최대 ~1분, status 가 `stopped` 로 전환.

**업그레이드**: 모듈 행 → `↑ 업그레이드` — 대상 버전을 고르면(등록된 버전이 여럿이면 입력을
묻는다) 배포의 대상 패키지를 그 버전으로 바꾸고 `upgrade` job 을 큐잉한다.
`설치/재설치` 는 **현재 지정된 버전을 다시 깔 뿐**이라 새 버전으로 올라가지 않는다 — 버전을
올릴 때는 이 버튼을 쓴다.

> ### 업그레이드는 **정지 상태**에서 한다
> 실행 중인 모듈은 409 로 막힌다(`force` 로 우회 가능). 먼저 [패키지 제어] 에서 정지해
> 서비스에서 뺀 뒤 올리고, 확인한 다음 다시 시작한다. 이 전제 하나가 모든 구성을 덮는다:
>
> | 구성 | 이 규칙이 강제하는 것 |
> |---|---|
> | Active/Standby (csp·csc·oam·oam-svc) | standby 는 이미 정지 상태 → **자연히 standby 먼저** |
> | All-Active (cmp·cmdp) | active/standby 개념이 없다. **한 대를 명시적으로 빼야** 올릴 수 있어 동시 다운이 막힌다 |
> | 단독 | 정지 → 업그레이드 → 시작 |
>
> **업그레이드는 모듈을 자동으로 시작하지 않는다** — 정지 상태였으면 파일만 갈아끼우고
> 그대로 둔다(기동은 [패키지 제어] 에서). 정지는 운영자의 의도이고, 업그레이드가 그것을
> 뒤집으면 트래픽을 뺀 노드가 제멋대로 서비스에 복귀한다. 반대로 (force 로) 실행 중인
> 모듈을 올리면 재기동한다 — 파일만 바꾸면 구 프로세스가 구 코드를 계속 실행하기 때문이다.

- 설정은 이전 버전에서 **이관**된다(collection jsonl + 배포 overlay). 새 설정 항목은 기본값.
- 구 버전 디렉터리는 보존(최신 3개)되므로 곧바로 `⤺ 롤백` 할 수 있다.
- 관리평면(oam/oam-svc)의 **ACTIVE** 는 별도 안내로 막는다 — 여기서는 "정지하세요" 가 틀린
  답이다(Active OAM 을 멈추면 콘솔이 사라져 지시할 통로가 없다). 순서는
  **standby 먼저 → 계획 절체 → 구 ACTIVE** 다.
- 버전 전환과 job 큐잉은 **서버가 한 번에** 처리한다(`POST /deployments/{id}/upgrade`).
  가드·검증을 모두 통과한 뒤에만 전환하므로, 막힌 요청은 배포 기록을 건드리지 않는다.

## 4. 설정 — [패키지 설정] 탭

설정의 편집 위치가 값의 성격으로 갈린다:

- **그룹 공통 설정** (타이머·정책·DB 접속 등 멤버 간 동일해야 하는 값) →
  **그룹 선택** → [패키지 설정] (§4.3)
- **서버 개별 설정** (bind IP·노드 식별자 등 서버 고유값) →
  **서버 선택** → [패키지 설정] (§4.1)

`pending`(설치 전) 배포도 설정 가능 — 저장값은 overlay 로 보존됐다가 설치 시 반영된다.

### 4.1 서버 개별 설정 (서버 선택, operator)

- AS 그룹 멤버 서버는 **이 서버 고유값만** 표시된다 — 공통 설정은 화면에 없고, 안내
  배너가 그룹 화면으로 유도한다. 배포 시 자동 주입되는 "인프라" 접힘 블록은 전부
  서버 개별이라 여기서만 보인다
- AA 그룹 멤버·standalone 서버는 동기화 개념이 없어 **전체 설정**을 여기서 편집
- 🔁 재기동 필요 / ⚡ 즉시 적용 표시, 변경한 필드는 ● 표시
- 저장 = 이 서버에만 적용, `update_config` job 1건 큐잉

### 4.2 collection 설정 (탭: "리스너" 등)

- deployment 가 **install 되어 있어야** 활성화 (pending 상태에선 에러)
- `＋ 추가` → 행 편집 → `저장` → Agent 의 jsonl 에 즉시 반영 + SIGUSR1 시그널
  (CSP 가 실행 중이면 즉시 rebind)
- AS 그룹의 **공통 컬렉션**(routes 등)은 그룹 화면(§4.3)에서 편집 — 동기화 ON 이면
  저장 직후 전 멤버로 전파된다. **개별 컬렉션**(local_nodes 등)은 서버 화면에서

### 4.3 그룹 공통 설정 + 동기화 스위치 (그룹 선택, AS 그룹)

**위치**: [패키지 설정] 탭 → **그룹 선택** → 패키지별 탭

- **동기화 스위치** (그룹×패키지, 기본 ON):
  - **ON** — 시스템이 실측 ACTIVE(VIP 보유) 서버를 기준으로 STANDBY 의 공통 설정을
    자동으로 맞춘다(이벤트 즉시 + 주기 1분). [공통 설정] 저장 = 전 멤버 적용
  - **OFF** — 자동 교정 정지, 멤버별 독립 편집 모드. [공통 설정] 상단에서 편집할
    멤버를 선택하고 저장하면 그 멤버에만 적용 (업데이트 작업 창)
- 상태줄에 실측 **ACTIVE** 서버·버전 혼재 경고·드리프트 대기 건수 표시
- **[멤버 비교]** — 멤버별 값을 나란히 비교: 🔗 공통+동일=정상 / ⚠ 드리프트(스위치
  ON 이면 자동 교정이 곧 해소) / 개별=서버 고유값. 셀 클릭 시 해당 서버 화면으로 이동
- 자동 교정 안전장치: ACTIVE 판정 불가(절체 직후 등)·멤버 버전 불일치 시 복사하지
  않고 대기. 개별(system) 값은 어떤 경우에도 복사되지 않는다

**롤링 업그레이드 절차** (S1=ACTIVE, S2=STANDBY, V1→V2):
1. 그룹 [패키지 설정]에서 동기화 스위치 **OFF**
2. S2 모듈을 [패키지 설치] → `↑ 업그레이드` 로 V2 전환 (standby 라 이미 정지 상태) —
   기존 설정 승계, 새 항목은 기본값. 업그레이드는 기동하지 않으므로 다음 단계에서 시작한다
3. 그룹 [공통 설정]에서 **멤버=S2** 선택 후 새/변경 항목 수정 (S2 고유값은 S2 서버
   화면에서) → S2 기동 → 절체 (콘솔 ACTIVE 뱃지가 30초 내 S2 로 바뀜)
4. 동기화 스위치 **ON** — S1 이 아직 V1 이므로 정합은 보류 상태로 대기
5. S1 을 `↑ 업그레이드` 로 V2 전환 → 버전이 일치하는 순간 **ACTIVE(S2)의 설정이 S1 로 자동
   복사** → 완료 (수동 동기화 불필요)

## 5. 시작 — [패키지 제어] 탭

**위치**: [패키지 제어] 탭 → 서버 선택 → 모듈 행 → `▶ 시작`

- `start` job → Agent 가 `install_path/cims.sh start <process>` 실행 → 성공 시 `running`
- `↻ 재시작` / `■ 정지` 도 같은 행 (미설치 pending 모듈은 비활성)
- 그룹 선택 시 **멤버 × 모듈 매트릭스** — 그룹 전체 프로세스 상태를 한눈에 보고 셀별 제어

## 6. 이후 운영

| 작업 | 탭 / 버튼 |
|---|---|
| 서버 개별 설정 변경 | [패키지 설정] → 서버 선택 |
| 그룹 공통 설정 변경 / 동기화 스위치 / 멤버 비교 | [패키지 설정] → 그룹 선택 |
| 프로세스 시작/재기동/중지 | [패키지 제어] → ▶ / ↻ / ■ |
| 모듈 재설치 (같은 버전) | [패키지 설치] → 설치/재설치 |
| 모듈 버전 업그레이드 | [패키지 설치] → ↑ 업그레이드 |
| 모듈 버전 롤백 | [패키지 설치] → ⤺ 롤백 |
| 모듈 제거 | [패키지 설치] → ✕ |
| 서버 이름 변경 | 서버 헤더의 이름 옆 `✎` |
| 서버 그룹 이름 변경 | 그룹 선택 → [메타] → 이름 → 적용 |
| 서버 세션 폐기 | 서버 헤더의 "폐기" |
| Agent 바이너리 업그레이드/롤백 | 서버 헤더의 "↑ 업그레이드" / "↓ 롤백" |

## 7. 문제 해결

| 증상 | 확인 |
|---|---|
| Agent status 가 `approved` 에서 `online` 으로 안 바뀜 | 대상 호스트에서 `systemctl --user status cims-agent` active 인지, `journalctl --user -u cims-agent` 로그, OAM URL 유효한지 |
| "＋ 모듈 추가" 에서 모듈 목록이 비어있음 | 1. 패키지 업로드 먼저 |
| 설정 저장 시 `not_installed` | 설치 버튼 먼저 눌러 deployment.install_path 생성 |
| 그룹 [멤버 비교]에서 ⚠ 드리프트 경고 | 동기화 스위치 ON 이면 자동 교정이 곧 해소(ACTIVE 판정·버전 일치 필요). 계속 남으면 상태줄의 보류 사유 확인. 의도적으로 서버마다 달라야 하는 값이면 템플릿의 해당 필드 scope 를 system 으로 조정 (개발 문의) |
| 가입자/조직 등 CSC 화면이 ApiError (502) | CSC 가 OAM(base)과 **다른 서버**에 배치된 경우, CSC 그룹 공통 설정의 `게이트웨이 접근 주소(Server.GatewayHost)` 에 그룹 VIP 를 입력 후 저장 (비우면 127.0.0.1 = 동거 배치 전용) |
| A/S 그룹에 Active/Standby 상태가 안 뜸 | **서비스 시작 전이면 정상** — HA 상태는 [서비스 제어]에서 모듈을 시작한 그룹에만 생긴다 (설치·VIP 구성만 한 상태에서는 keepalived 가 무장되지 않음) |
| Collection 탭에서 `agent_proxy_failed` | Agent 가 heartbeat 보내 `sync_port` 가 기록됐는지, 방화벽 9900 포트 |
| `signaled:[]` 로 반환 (빈 배열) | `install_path/run/*.pid` 파일이 없음. CSP 가 pid 를 쓰도록 실행 중이어야 함 |
