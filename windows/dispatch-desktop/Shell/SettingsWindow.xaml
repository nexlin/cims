<Window x:Class="DispatchDesktop.Shell.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DispatchDesktop.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
        Style="{StaticResource Window.Base}" Title="설정" Width="640" Height="640" WindowStartupLocation="CenterOwner">
    <!-- 설정 — 오디오(§7)·핫키(§8)·관제·표시·채널 -->
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Style="{StaticResource Btn}" Content="닫기" IsCancel="True" Margin="0,0,6,0" />
            <Button Style="{StaticResource Btn.Pri}" Content="저장·적용" Command="{Binding SaveCommand}" />
        </StackPanel>
        <TabControl>
            <TabItem Header="오디오">
                <StackPanel Margin="12">
                    <DockPanel Margin="0,0,0,6">
                        <Button DockPanel.Dock="Right" Style="{StaticResource Btn.Sm}" Content="다시 읽기" Command="{Binding LoadDevicesCommand}" />
                        <TextBlock Style="{StaticResource Hint}" Text="장치는 이름으로 기억합니다(id 는 재부팅마다 바뀔 수 있음). 빈 값 = 시스템 기본 장치." VerticalAlignment="Center" />
                    </DockPanel>
                    <TextBlock Text="마이크(캡처)" Style="{StaticResource Hint}" />
                    <ComboBox Style="{StaticResource FieldCombo}" ItemsSource="{Binding CaptureDevices}" SelectedItem="{Binding CaptureDevice}" IsEditable="True" Text="{Binding CaptureDevice}" Margin="0,2,0,10" />
                    <TextBlock Text="헤드셋(라우트 0) — 통화·VoLTE 감청·사설콜 기본" Style="{StaticResource Hint}" />
                    <ComboBox Style="{StaticResource FieldCombo}" ItemsSource="{Binding RenderDevices}" SelectedItem="{Binding HeadsetDevice}" IsEditable="True" Text="{Binding HeadsetDevice}" Margin="0,2,0,10" />
                    <CheckBox Content="데스크 스피커 라우트 사용 (PTT 그룹·애드혹·PTT 청취 기본)" IsChecked="{Binding SpeakerRouteEnabled}" Margin="0,0,0,4" />
                    <ComboBox Style="{StaticResource FieldCombo}" ItemsSource="{Binding RenderDevices}" SelectedItem="{Binding SpeakerDevice}" IsEditable="True" Text="{Binding SpeakerDevice}" IsEnabled="{Binding SpeakerRouteEnabled}" Margin="0,2,0,10" />
                    <CheckBox Content="장치가 다시 붙으면 자동 복귀" IsChecked="{Binding AutoReturnToPreferredDevice}" />
                    <TextBlock Style="{StaticResource Hint}" Margin="0,10,0,0" Text="카드·감청 창의 🎧/🔊 토글은 세션별 라우트를 바꿉니다. 스피커 라우트가 없으면 비활성." />
                </StackPanel>
            </TabItem>
            <TabItem Header="핫키">
                <StackPanel Margin="12">
                    <TextBlock Style="{StaticResource Hint}" Text="예: Ctrl+Space, F9, Ctrl+Shift+P. 전역 키 등록 실패(다른 프로그램과 충돌)는 빨강으로 표시되며 앱 포커스에서만 동작합니다." Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding HotKeys}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:HotKeyRow}">
                                <Grid Margin="0,3">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="200" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Label}" VerticalAlignment="Center" />
                                    <TextBox Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource Field}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Conflict}" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource Brush.Emg}" /><Setter Property="Foreground" Value="{DynamicResource Brush.Emg}" /></DataTrigger>
                                                    <DataTrigger Binding="{Binding IsValid}" Value="False"><Setter Property="BorderBrush" Value="{DynamicResource Brush.Emg}" /></DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource Hint}">
                                                <Setter Property="Text" Value="전역" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsGlobal}" Value="False"><Setter Property="Text" Value="앱 포커스 시" /></DataTrigger>
                                                    <DataTrigger Binding="{Binding Conflict}" Value="True"><Setter Property="Text" Value="충돌 — 다른 프로그램이 이미 사용" /><Setter Property="Foreground" Value="{DynamicResource Brush.Emg}" /></DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Style="{StaticResource Hint}" Margin="0,8,0,0" Text="채널 선택 Ctrl+1..9 는 고정." />
                </StackPanel>
            </TabItem>
            <TabItem Header="관제">
                <StackPanel Margin="12">
                    <TextBlock Text="당겨받기 피처코드 (접속서비스 pickup_feature_code)" Style="{StaticResource Hint}" />
                    <TextBox Style="{StaticResource Field}" Text="{Binding PickupFeatureCode, UpdateSourceTrigger=PropertyChanged}" Width="120" HorizontalAlignment="Left" Margin="0,2,0,10" />
                    <CheckBox Content="활성 통화 중 새 착신 응답 시 기존 통화 자동 보류" IsChecked="{Binding AutoHoldOnAnswer}" Margin="0,0,0,6" />
                    <CheckBox Content="감청 창 닫기 전 확인" IsChecked="{Binding ConfirmCloseMonitor}" Margin="0,0,0,6" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="동시 청취 상한" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Style="{StaticResource Field}" Text="{Binding MaxMonitorWindows, UpdateSourceTrigger=PropertyChanged}" Width="60" />
                    </StackPanel>
                    <CheckBox Content="채널 카드를 선택하면 MCData 스레드가 따라감" IsChecked="{Binding FollowChannelThread}" Margin="0,0,0,6" />
                    <CheckBox Content="창 닫기는 트레이(작업 표시줄) 최소화" IsChecked="{Binding MinimizeToTray}" Margin="0,0,0,6" />
                    <CheckBox Content="Windows 시작 시 자동 실행" IsChecked="{Binding AutoStart}" Margin="0,0,0,6" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="메시지 보관 일수" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Style="{StaticResource Field}" Text="{Binding MessageRetentionDays, UpdateSourceTrigger=PropertyChanged}" Width="60" />
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="채널·주소록">
                <StackPanel Margin="12">
                    <TextBlock Text="카드로 보일 멤버 그룹 (전부 선택 = 멤버 그룹 전부)" Style="{StaticResource Hint}" Margin="0,0,0,6" />
                    <ItemsControl ItemsSource="{Binding Channels}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:ChannelChoice}">
                                <CheckBox IsChecked="{Binding Selected}" Margin="0,2">
                                    <TextBlock><Run Text="{Binding Group.Name, Mode=OneWay}" /><Run Text=" (멤버 " Foreground="{DynamicResource Brush.Muted}" /><Run Text="{Binding Group.MemberCount, Mode=OneWay}" Foreground="{DynamicResource Brush.Muted}" /><Run Text=")" Foreground="{DynamicResource Brush.Muted}" /></TextBlock>
                                </CheckBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="주소록 CSV (kind,number,name,tags — ext|external|ptt, tags 의 member = 관제 그룹원)" Style="{StaticResource Hint}" Margin="0,14,0,2" />
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Style="{StaticResource Btn}" Content="찾기…" Command="{Binding BrowseDirectoryCommand}" Margin="6,0,0,0" />
                        <TextBox Style="{StaticResource Field}" Text="{Binding DirectoryCsv, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                    <TextBlock Style="{StaticResource Hint}" Margin="0,4,0,0"><Run Text="현재: " /><Run Text="{Binding DirectoryLoadedFrom, Mode=OneWay}" /></TextBlock>
                </StackPanel>
            </TabItem>
            <TabItem Header="표시·로그">
                <StackPanel Margin="12">
                    <TextBlock Text="테마" Style="{StaticResource Hint}" />
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,10">
                        <RadioButton Content="밝게" IsChecked="{Binding Theme, Converter={StaticResource Eq}, ConverterParameter=light}" Margin="0,0,12,0" />
                        <RadioButton Content="어둡게" IsChecked="{Binding Theme, Converter={StaticResource Eq}, ConverterParameter=dark}" />
                    </StackPanel>
                    <TextBlock Text="코어(pjsip) 로그 레벨 1~6" Style="{StaticResource Hint}" />
                    <TextBox Style="{StaticResource Field}" Text="{Binding LogLevel, UpdateSourceTrigger=PropertyChanged}" Width="60" HorizontalAlignment="Left" Margin="0,2,0,10" />
                    <TextBlock Style="{StaticResource Hint}"><Run Text="로그 위치: " /><Run Text="{Binding LogsPath, Mode=OneWay}" /></TextBlock>
                </StackPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
