<UserControl x:Class="DispatchDesktop.Views.CallActivityPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DispatchDesktop.ViewModels"
             xmlns:m="clr-namespace:DispatchDesktop.Models"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:CallActivityViewModel}">
    <!-- ④ 일반통화 내역 — 실시간 (§4.4): 진행 중 세션 행 + 최근 기록 -->
    <Border Style="{StaticResource Pnl}">
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}">
                <DockPanel VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource H2}" Text="④ 일반통화 내역" Margin="6,0,10,0" />
                    <TextBlock Style="{StaticResource Muted}" Text="실시간" VerticalAlignment="Center" />
                    <Button DockPanel.Dock="Right" Style="{StaticResource Btn.Xs}" Content="CSV" Command="{Binding ExportCommand}" Margin="6,0,0,0" />
                    <TextBox DockPanel.Dock="Right" Style="{StaticResource Field}" Width="140" Height="26" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" ToolTip="검색" />
                    <Border Style="{StaticResource SegGroup}" HorizontalAlignment="Right" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Style="{StaticResource Seg}" Content="전체" IsChecked="{Binding Filter, Converter={StaticResource Eq}, ConverterParameter=all}" Command="{Binding SetFilterCommand}" CommandParameter="all" />
                            <RadioButton Style="{StaticResource Seg}" Content="대표번호" IsChecked="{Binding Filter, Converter={StaticResource Eq}, ConverterParameter=pilot}" Command="{Binding SetFilterCommand}" CommandParameter="pilot" />
                            <RadioButton Style="{StaticResource Seg}" Content="부재" IsChecked="{Binding Filter, Converter={StaticResource Eq}, ConverterParameter=missed}" Command="{Binding SetFilterCommand}" CommandParameter="missed" />
                        </StackPanel>
                    </Border>
                </DockPanel>
            </Border>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource LogHeader}"><TextBlock Style="{StaticResource Hint}" Text="진행 중" /></Border>
                    <ItemsControl ItemsSource="{Binding Ongoing}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:CallSessionRow}">
                                <Border Style="{StaticResource LogRow}">
                                    <DockPanel>
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                            <Button Style="{StaticResource Btn.Xs}" Content="지정 픽업" Command="{Binding PickupCommand}" Visibility="{Binding CanPickup, Converter={StaticResource BoolToVis}}" />
                                            <Button Style="{StaticResource Btn.Xs.Mon}" Content="청취" Command="{Binding MonitorCommand}" IsEnabled="{Binding CanMonitor}" ToolTip="{Binding MonitorTip}" Visibility="{Binding IsMonitoring, Converter={StaticResource NotToVis}}" />
                                            <Button Style="{StaticResource Btn.Xs.Mon}" Content="청취 중 · 창" Command="{Binding ShowWindowCommand}" Visibility="{Binding IsMonitoring, Converter={StaticResource BoolToVis}}" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource MonoMuted}" Text="{Binding Elapsed, Converter={StaticResource Elapsed}}" Width="54" />
                                            <TextBlock FontWeight="Medium" Margin="0,0,8,0"><Run Text="{Binding A, Mode=OneWay}" /><Run Text=" ↔ " Foreground="{DynamicResource Brush.Muted}" /><Run Text="{Binding B, Mode=OneWay}" /></TextBlock>
                                            <Border Style="{StaticResource Badge.Pilot}" Visibility="{Binding PathBadge, Converter={StaticResource StrToVis}}"><TextBlock Text="{Binding PathBadge}" /></Border>
                                            <Border Style="{StaticResource Badge}" Visibility="{Binding IsTalking, Converter={StaticResource BoolToVis}}"><TextBlock Text="{Binding VisibilityBadge}" /></Border>
                                            <Ellipse>
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse" BasedOn="{StaticResource Dot}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsRinging}" Value="True"><Setter Property="Fill" Value="{DynamicResource Brush.Ring}" /></DataTrigger>
                                                            <DataTrigger Binding="{Binding IsTalking}" Value="True"><Setter Property="Fill" Value="{DynamicResource Brush.Talk}" /></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <TextBlock Style="{StaticResource Muted}" Text="{Binding StateText}" FontSize="12" />
                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Style="{StaticResource Hint}" Text="진행 중인 통화 없음" Margin="10,6" Visibility="{Binding Ongoing.Count, Converter={StaticResource ZeroToVis}}" />
                    <Border Style="{StaticResource LogHeader}"><TextBlock Style="{StaticResource Hint}" Text="최근" /></Border>
                    <ItemsControl ItemsSource="{Binding Recent}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type m:ActivityRow}">
                                <Border Style="{StaticResource LogRow}">
                                    <DockPanel>
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Visibility="{Binding CanRedial, Converter={StaticResource BoolToVis}}">
                                            <Button Style="{StaticResource Btn.Xs}" Content="재발신" Command="{Binding DataContext.RedialCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,4,0" />
                                            <Button Style="{StaticResource Btn.Xs}" Content="문자" Command="{Binding DataContext.SmsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                        </StackPanel>
                                        <TextBlock Style="{StaticResource MonoMuted}" Text="{Binding Time, StringFormat=HH:mm}" Width="54" />
                                        <Border Style="{StaticResource Badge}" Width="52"><TextBlock Text="{Binding KindText}" HorizontalAlignment="Center" /></Border>
                                        <TextBlock Text="{Binding Title}" Margin="0,0,8,0" TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers><DataTrigger Binding="{Binding IsMissed}" Value="True"><Setter Property="Foreground" Value="{DynamicResource Brush.Emg}" /></DataTrigger></Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Style="{StaticResource Muted}" Text="{Binding Detail}" TextTrimming="CharacterEllipsis" />
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
