<UserControl x:Class="DispatchDesktop.Views.PttActivityPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DispatchDesktop.ViewModels"
             xmlns:m="clr-namespace:DispatchDesktop.Models"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PttActivityViewModel}">
    <!-- ⑤ PTT 이벤트(§4.4) — 방금 일어난 것의 흐름(진행 중 행 없음). 머리 [● 따라가기] · [전체|발언|긴급] · 검색 · [이력에서 보기] · CSV. 활성 긴급 행은 위에 고정 -->
    <Border Style="{StaticResource Pnl}">
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}">
                <DockPanel VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource H2}" Text="⑤ PTT 이벤트" Margin="6,0,10,0" />
                    <Button DockPanel.Dock="Right" Style="{StaticResource Btn.Xs}" Content="CSV" Command="{Binding ExportCommand}" Margin="6,0,0,0" />
                    <Button DockPanel.Dock="Right" Style="{StaticResource Btn.Xs}" Content="이력에서 보기" Command="{Binding OpenHistoryCommand}" Margin="6,0,0,0" ToolTip="끝난 PTT 세션의 날짜별 조회·녹취 — [이력] 화면(F2)" />
                    <TextBox DockPanel.Dock="Right" Style="{StaticResource Field}" Width="120" Height="26" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" ToolTip="검색" />
                    <Border DockPanel.Dock="Right" Style="{StaticResource SegGroup}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Style="{StaticResource Seg}" Content="전체" IsChecked="{Binding Filter, Converter={StaticResource Eq}, ConverterParameter=all, Mode=OneWay}" Command="{Binding SetFilterCommand}" CommandParameter="all" />
                            <RadioButton Style="{StaticResource Seg}" Content="발언" IsChecked="{Binding Filter, Converter={StaticResource Eq}, ConverterParameter=talk, Mode=OneWay}" Command="{Binding SetFilterCommand}" CommandParameter="talk" />
                            <RadioButton Style="{StaticResource Seg}" Content="긴급" IsChecked="{Binding Filter, Converter={StaticResource Eq}, ConverterParameter=emergency, Mode=OneWay}" Command="{Binding SetFilterCommand}" CommandParameter="emergency" />
                        </StackPanel>
                    </Border>
                    <!-- 따라가기 토글 -->
                    <Button HorizontalAlignment="Left" Command="{Binding ToggleFollowCommand}" ToolTip="포커스 채널의 이벤트만 / 끄면 범위 전체">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource Btn.Xs}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding FollowFocus}" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource Brush.Primary}" /><Setter Property="Foreground" Value="{DynamicResource Brush.Primary}" /><Setter Property="Background" Value="{DynamicResource Brush.PrimarySoft}" /></DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="7" Height="7" Margin="0,0,5,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{DynamicResource Brush.Wire}" />
                                        <Style.Triggers><DataTrigger Binding="{Binding FollowFocus}" Value="True"><Setter Property="Fill" Value="{DynamicResource Brush.Primary}" /></DataTrigger></Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding FollowText}" />
                        </StackPanel>
                    </Button>
                </DockPanel>
            </Border>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- 활성 긴급 고정 행 -->
                    <ItemsControl ItemsSource="{Binding Pinned}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:PinnedEmergency}">
                                <Border Style="{StaticResource LogRow}" Background="{DynamicResource Brush.EmgSoft}">
                                    <DockPanel>
                                        <Button DockPanel.Dock="Right" Style="{StaticResource Btn.Xs.Danger}" Content="채널로" Command="{Binding DataContext.ChannelPinnedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                        <TextBlock Style="{StaticResource MonoMuted}" Text="{Binding Elapsed, Converter={StaticResource Elapsed}}" Width="54" />
                                        <Border Style="{StaticResource Badge.Emg}" Width="52"><TextBlock Text="{Binding KindText}" HorizontalAlignment="Center" /></Border>
                                        <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource Brush.Emg}"><Run Text="{Binding Title, Mode=OneWay}" /><Run Text=" 진행 중" /></TextBlock>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ItemsControl ItemsSource="{Binding Recent}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type m:ActivityRow}">
                                <Border x:Name="Row" Style="{StaticResource LogRow}" Cursor="Hand" MouseLeftButtonUp="Row_Click">
                                    <DockPanel>
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                            <Button x:Name="ReplyBtn" Style="{StaticResource Btn.Xs}" Content="답장" Visibility="Collapsed" Command="{Binding DataContext.ReplyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="④ 스레드로" />
                                        </StackPanel>
                                        <TextBlock Style="{StaticResource MonoMuted}" Text="{Binding Time, StringFormat=HH:mm}" Width="54" />
                                        <Border Style="{StaticResource Badge}" Width="52"><TextBlock Text="{Binding KindText}" HorizontalAlignment="Center" /></Border>
                                        <TextBlock Text="⚠" Foreground="{DynamicResource Brush.Emg}" Margin="0,0,6,0" Visibility="{Binding IsEmergency, Converter={StaticResource BoolToVis}}" />
                                        <TextBlock Text="{Binding Title}" Margin="0,0,8,0" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Style="{StaticResource Muted}" Text="{Binding Detail}" TextTrimming="CharacterEllipsis" />
                                    </DockPanel>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Kind}" Value="Sds"><Setter TargetName="ReplyBtn" Property="Visibility" Value="Visible" /></DataTrigger>
                                    <Trigger SourceName="Row" Property="IsMouseOver" Value="True"><Setter TargetName="Row" Property="Background" Value="{DynamicResource Brush.Hover}" /></Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Style="{StaticResource Hint}" Text="이벤트 없음" Margin="10,6" Visibility="{Binding Recent.Count, Converter={StaticResource ZeroToVis}}" />
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
