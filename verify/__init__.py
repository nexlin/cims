"""CIMS 검증 도구 패키지.

- `verify.lib` — 검증 인프라 (registry / runner / context / reporting / shell / presets / common / items)
- CLI 진입점 — `tests/cims_verify.py` (`python3 -m tests.cims_verify`)
- 단위 테스트 — `tests/test_verify_lib.py`
"""
