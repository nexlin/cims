# R1 리팩토링 진행 리포트 — primary local_node 자동 주입

- **일시**: 2026-04-23 17:30–19:15
- **브랜치**: feature/sip-console-runtime
- **커밋 예정**: `feat(config+R1): build/configure/pkg 3단계 분리 + CSP 설정 구조 통합 + primary local_node 자동 주입`
- **환경 IP**: `ens160 = 192.168.0.2` (DHCP)
- **DB**: MariaDB localhost cims/cims1234

---

## 스코프

psip + CSP 리팩토링 전체 계획 (R1~R7) 중 **R1 단계** — `Setup.Sip.LocalIp` / `Setup.Sip.UdpPort` 가 CSP 인스턴스 primary identity 임을 코드와 스키마에 명확화. local_nodes 의 primary 항목에서 자동 유도되도록 구조 재배치.

리팩토링 전체 배경:
- `Setup.Sip.LocalIp` 은 단순 bootstrap 이 아니라 From/Via/Contact 생성 에 쓰이는 CSP 전역 identity (`CspServer.cpp:100-106, 294, 332, 360, 376, 380` 100+ 사용).
- psip 는 Transport 멀티-리스너(UDP)를 이미 지원하지만 CSP 가 global LocalIp 하나로 Via/Contact 를 생성하는 구조적 한계.
- 새 9-collection 모델에 맞춰 R1~R7 단계별 리팩토링 합의.

---

## 변경 파일

| 파일 | 변경 |
|---|---|
| `csp/CspLocalNodeMap.h` | `LocalNodeInfo::is_primary` 필드 + `GetPrimary()` 선언 |
| `csp/CspLocalNodeMap.cpp` | `Sync()` 에서 `is_primary` 파싱, `GetPrimary()` 규칙 1/2/3 구현 |
| `csp/CspServer.cpp` | ConfigCache + LocalNodeMap 선로드 + primary → gclsSetup 주입 블록 추가, line 224 중복 `Sync()` 제거 |
| `csp/config/config_template.json` | `sip` 섹션에서 LocalIp/UdpPort 제거 → `_infra` 로 이동 (deploy_value 유지), `local_nodes` schema 에 `is_primary` 필드 추가 |
| `build/dist/config/local_nodes.jsonl` (seed) | 기존 `volte-access` 에 `"is_primary": true` 명시 |

함께 커밋되는 그룹 A (이전 세션에서 이어받은 기반 정리):

| 파일 | 변경 |
|---|---|
| `cims.sh` | build / configure / pkg 3단계 명확 분리 (deprecated 옵션 제거) |
| `configure.sh` | `apply_config_template()` Python 기반 렌더러 추가 |
| `csp/config/csp.json.template` | 삭제 (SOT 통합) |
| `csc/src/handlers/{agents,modules,stats}.py` | dist-first / DB fallback 모듈/패키지 로딩 |
| `cims-console/src/components/module/ModuleConfigEditor.tsx`, `ModuleConfigModal.tsx` | React.memo + useMemo 로 편집 중 rerender 방지 |
| `docs/VERIFICATION_PROCESS.md` | 3단계 (`[1/3] build → [2/3] configure → [3/3] pkg`) 표기 반영 |

---

## 검증 결과

### (1) 빌드
- `cmake --build build -j` 성공. `csp` 바이너리 링크 완료.
- warning: 기존 `CallDir.h:524` snprintf truncation 만 존재 (R1 무관).
- `make dist` → `build/dist/csp/config/config_template.json` 과 바이너리 갱신 확인.

### (2) Primary 경로
```
[SYSTEM] CspConfigCache: loaded local_node from .../local_nodes.jsonl (1 records)
[INFO]   LocalNodeMap: sync complete, 1 nodes
[SYSTEM] primary local_node 'volte-access' → LocalIp=192.168.0.2 UdpPort=5060
[SYSTEM] SipServer started successfully.
```
- `ss -luntp`: `192.168.0.2:5060` UDP UNCONN, `0.0.0.0:5061` TCP LISTEN, `0.0.0.0:25061` TCP LISTEN
- ERROR/FATAL 0 건

### (3) Fallback 경로 (local_nodes.jsonl 제거 시)
```
[INFO]   CspConfigCache: local_node jsonl absent — empty
[INFO]   LocalNodeMap: sync complete, 0 nodes
[INFO]   no primary local_node — using _infra Setup.Sip.LocalIp=192.168.0.2 UdpPort=5060
[SYSTEM] SipServer started successfully.
```
- fallback 값으로도 5060 LISTEN 정상. SipServer 기동 성공.

### (4) SIP 메시지 송수신
- cspsim 의 REGISTER → 401 Unauthorized (challenge) → Authorization 재시도 → 403 Forbidden 흐름 관측
- Via/From/Contact 에 `192.168.0.2:5060` 사용 정상

### (5) REGISTER smoke 결과 (보조)
- `-user 450033100000001 -password 123456 -domain ims.mnc033.mcc450.3gppnetwork.org` 로 시도 → **403 Forbidden**
- 이전 세션 (bc19f2f 시점) 에서는 동일 조합 PASS 기록
- **R1 변경과 무관**: SIP 레이어 정상, CSCF 가 Digest 응답 검증 후 거부 — DB password hash 불일치 의심
- 다음 세션 R7 회귀 전 `CSC API /api/v1/volte/subscriptions` 로 실제 password 확인 필요

---

## dist 정리 부산물
- `build/dist/csp/bin/` 안의 깨진 UTF-8 이름 디렉토리 4개 (2026-04-13 18:56 일회성 flow/msg jsonl 잔재) 제거.

---

## 다음 세션 진입점

### (1순위) R2 — UDP multi-listener per-listener thread_count
- `LocalNodeInfo` + `config_template.json` 의 `local_nodes` schema 에 `thread_count` 추가
- `CspListenerManager.Sync` 에서 `psip::AddUdpListener` 호출 시 per-listener thread_count 전달
- 두 개 local_node (다른 bind_port) 기동 smoke + 메시지 송수신 확인

### (2순위) R3 (psip TCP/TLS multi-listener API 확장) 이후 순차

### 환경 사전 체크리스트 (세션 시작 시)
- [ ] `ip -4 addr show ens160` 으로 현재 DHCP IP 확인
- [ ] `cims.sh configure --local-ip <ens160_ip> --db-password cims1234`
- [ ] REGISTER credential 확인: `CSC API /api/v1/volte/subscriptions` (현재 password `123456` 이 맞는지)
- [ ] 메모리 `project_phase_status.md` + `project_psip_csp_refactor.md` 읽기
