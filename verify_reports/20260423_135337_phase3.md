# Phase 3 Verification Report

- Timestamp: 20260423_135337
- Host: nex-ubuntu
- ens160 IP: 192.168.68.116
- Git: feature/sip-console-runtime @ bc19f2f (uncommitted)
- 실행 방식: **근사 Phase 3** — TB 3종 미구축 상태에서 Phase 1 빌드 CSC(4420)/Console(3001) 을 "New-CSC/New-Console 역할 대체" 로 간주하여 배포 체인만 검증

## 0. Phase 3 목적
Phase 2 에서 배포된 New-CSC(4420) 가 다시 CSP/CMP/Sim 을 배포할 때의 **체인 동작** 검증. 기능 회귀 없음 (Phase 1 에서 완료).

## 1. 사전 조건
- Phase 1 통과 (`verify_reports/20260423_123110_phase1.md`)
- CSC(4420) / Console(3001) / cwrtc / Phone 기동 중
- 패키지 8종 DB 등록 (file_size+sha256+meta_json 정상, csp/cmp/csc 는 config_template_json 포함)
- CSP(5060)/CMP(9000) 는 agent 경유 재배포를 위해 Phase 1 인스턴스를 정지 (§5)

## 2. Agent 설치 + enroll
- Agent 레코드 생성: `POST /api/v1/agents {"name":"phase3-agent-1"}` → id=3, enrollment_token 발급
- install-agent.sh 실행: `/tmp/cims-agent-p3/` 에 `cims_agent.py`, `run.sh`, `state/state.json` 설치
- Agent 백그라운드 기동(`nohup ./run.sh`) → enroll 성공 → heartbeat/metric 전송
- `POST /api/v1/agents/3/approve` 로 `pending → approved` 전환
- 결과: **status=approved, sync REST 9900 listening, heartbeat OK** ✓

## 3. CSP/CMP 인스턴스 정지
- `./cims.sh stop csp` / `./cims.sh stop cmp` — 5060/9000 포트 해제
- 유지: CSC(4420), Console(3001), cwrtc(8080), Phone(3000), Agent(9900)

## 4. 패키지 배포 (agent 경유)
| dep | pkg | install_path | 결과 |
|---|---|---|---|
| 1 | cmp@0.0.1 | /tmp/cims-agent-p3/modules/cmp | rc=0 |
| 2 | csp@0.0.1 | /tmp/cims-agent-p3/modules/csp | rc=0 |
| 3 | cspsim@0.0.1 | /tmp/cims-agent-p3/modules/cspsim | rc=0 |

각 deployment 마다:
1. `POST /api/v1/deployments` → agent_deployment 레코드 생성 (status=pending)
2. `POST /api/v1/deployments/{id}/job {"job_type":"install"}` → agent_job 큐잉
3. Agent heartbeat 에서 pickup → tarball 다운로드 → `install_path/{pkg}/` 에 해제 → config_template.json 렌더 → `install_path/{pkg}/config/{pkg}.json` 생성
4. Agent job report: status=200, rc=0
5. Deployment status = `stopped` (install 완료, 미기동)

## 5. 모듈 기동
`POST /api/v1/deployments/{id}/job {"job_type":"start"}` 호출 후 agent 가 install_path/cims.sh 를 실행.

| 모듈 | pid | 리슨 | 결과 |
|---|---|---|---|
| CMP | 139370 | 0.0.0.0:9000/udp | ✓ "Server listening on 0.0.0.0:9000" |
| CSP | 140341 | 192.168.68.116:5060/udp + 25061/tcp + 5061/tcp | ✓ Roles CSCF/TAS/PTT-AS=ON, IBCF=OFF |

CSP 기동 시 2개 그룹(+82571910001, +82571910002) DB 로드, CMP AddGroup 성공(floor=54016/54018, video=56016/56018), GMS NOTIFY 발송, CMP HEARTBEAT trans_id=26 OK.

## 6. 배포 체크리스트 (§2.4/§3.2)

| 항목 | 결과 | 근거 |
|---|---|---|
| agent enroll | ✓ | id=3, status=approved, session_token 발급 |
| heartbeat | ✓ | last_heartbeat 갱신, metric 전송 |
| 패키지 해시 | ✓ | DB `cims_package` 에 sha256 기록됨 (cmp=bb98d54b..., csp=348c7472..., cspsim=f9b333aa...) |
| install_path 생성 | ✓ | 3개 모듈 모두 `/tmp/cims-agent-p3/modules/{cmp,csp,cspsim}/` 하위에 pkg.json/config/binary 배치 |
| scalar config overlay | ⚠️ | `PUT /api/v1/deployments/{id}/config` 로 overlay 전달은 정상 (`install_path/config.json` 에 저장). 단 **실제 모듈 config(`install_path/{pkg}/config/{pkg}.json`) 로 merge 되지 않음** — agent 가 install 시점에만 template 렌더링하는 설계. 재렌더링 trigger 없음. 리포트 기록. |
| collection 전달 | ✓ | `PUT /api/v1/deployments/2/collection/access_services` → agent `/collection` 프록시 → `install_path/config/access_services.jsonl` 쓰기 + CSP pid 140341 로 SIGUSR1 시그널. CSP 로그: "ServiceMap: sync complete, 2 services" |
| 모듈 기동 (리슨) | ✓ | UDP 5060 + TCP 5061/25061 (CSP), UDP 9000 (CMP) |
| ERROR/FATAL | ✓ | CSP/CMP 로그에 ERROR/FATAL 0건 (REGISTER 403 Auth reject 는 collection 푸시 전 1회 발생, 푸시 후 해소) |
| TB-CSC → 대상 CSC HEARTBEAT | N/A | 근사 Phase 3 — TB-CSC 없음 |

## 7. Smoke-test REGISTER
- CSPSim 경로: `/tmp/cims-agent-p3/modules/cspsim/cspsim/bin/cspsim`
- Credential: user `+821357007001` / imsi `450033100000001` / passwd `123456` (DB volte_subscriptions 기준)
- 1차 시도 (collection 미푸시): **403 Forbidden** — CSP 로그 `Auth reject: ... service_ref='volte' imsi='450033100000001' — 데이터 불완전` (access_services.jsonl 없음)
- 2차 시도 (access_services PUT + SIGUSR1 후, 정정된 password):
  ```
  Registered   : 1 / 1  (fail=0)
  Avg Reg Time : 1ms
  ```
  ✓ REGISTER 성공 — 배포된 CSP 가 SIP 트래픽 처리 가능함을 실증.

## 8. 발견된 gap / 개선 제안

| # | 이슈 | Severity | 조치 |
|---|---|---|---|
| P3-01 | Phase 3 의 "collection 자동 전달" 미구현 — install job 시 `access_services.jsonl` 등 9 entity 를 자동으로 install_path/config/ 로 푸시하지 않음. 사용자가 `PUT /collection/{name}` 을 명시 호출해야 기능 동작. | Major | Phase 2 구현 시 `_create_deployment` 또는 `install job params` 에 기본 collection 세트 자동 첨부. CSC 에서 "해당 모듈이 요구하는 collection 목록"을 meta.json 에 선언하는 구조 필요. |
| P3-02 | scalar config overlay 가 `install_path/config.json` 에만 저장되고 실제 `install_path/{pkg}/config/{pkg}.json` 에 merge 되지 않음. `PUT /config` 후 restart 해도 변경 미반영. | Major | `job_update_config` 또는 process restart 경로에서 template + overlay 재렌더링 추가. Phase 1 용 `configure.sh apply_config_template` 의 dotted-key merge 로직을 agent 측에도 포트. |
| P3-03 | install 된 CSP/CMP 의 `ServiceLogging.Dir` 이 원본 호스트 경로(`/home/nex/work/cims/build/dist/ext_mnt/service_log`) 를 그대로 사용. install_path 와 분리되지 않음 — 다중 호스트 시 충돌 가능. | Minor | template 에서 SERVICE_LOG_DIR 치환 시 `install_path/../service_log` 또는 agent 가 감지하는 기본 경로로 변경. |
| P3-04 | TB-CSC(4419)/TB-Console(3000)/TB-agent(9902) 3종 및 `cims.sh verify phase2`/`verify phase3` 자동화 미구현. 현 Phase 3 는 수동 실행 + 기능 회귀(REGISTER) 는 "smoke" 정의에 맞게 1건만 수행. | Major | 별도 세션에서 TB 3종 구축 + `cims.sh verify phase2`/`verify phase3` 자동화 추가 필요. |

## 9. 결론
**배포 체인 자체는 정상 동작** — agent enroll + install + start + heartbeat + (수동) collection 전달을 통해 Phase 1 과 동일한 형태의 CSP/CMP 서비스가 기동되고 실제 REGISTER 트래픽을 처리한다.

단, 문서 §2.4/§3.2 에서 요구하는 자동 **collection 전달** 과 **config overlay 반영** 은 현 구현에서 누락되어 사용자 수동 개입이 필요하다. P3-01, P3-02 를 우선 처리 대상으로 기록.

### 미수행 항목 (근사 Phase 3 제약)
- PTT 배포/테스트 (Phase 3 는 smoke 1건으로 충분)
- TB-CSC → 대상 CSC 상호 heartbeat (TB 미구축)
- mTLS cert rotation e2e (Phase 1 N/A 와 동일 — AgentMtls 미설정)
